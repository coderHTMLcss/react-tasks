export enum ADMIN_ROUTES_NAME {
    Home = "/",
    Products = "/products",
    Users = "/users",
    Sessions = "/sessions",
}

export enum USER_ROUTES_NAME {
    Home = "/",
    Products = "/products",
    Product = "/products/:id",
}


export enum GUEST_ROUTES_NAME {
    Home = "/",
}

export enum COMMON_ROUTES_NAMES {
    Home = '/',
    Products = "/products",
}